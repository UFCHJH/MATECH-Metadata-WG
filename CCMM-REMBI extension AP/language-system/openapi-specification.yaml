{
  "openapi": "3.0.0",
  "info": {
    "title": "Your API Title",
    "version": "1.0.0",
    "description": "Your API Description"
  },
  "paths": {
    "/language_system": {
      "parameters": [
        {
          "name": "label",
          "in": "path",
          "description": "Language label",
          "required": 0,
          "schema": {
            "type": "string"
          }
        }
      ],
      "get": {
        "summary": "A system of signs, symbols, sounds, gestures, or rules used in communication, e.g. a language.",
        "description": "A system of signs, symbols, sounds, gestures, or rules used in communication, e.g. a language.",
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "example": {}
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "language_system": {
        "type": "object",
        "properties": {
          "label": {
            "description": "Language label",
            "type": "string"
          }
        }
      }
    }
  }
}